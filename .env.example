# Netlify Environment Variables for CXI Project
# Copy this file to .env for local development

# GitHub Integration (Required for saveFeedback function)
# Get a personal access token from: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public repos)
GITHUB_TOKEN=your_github_personal_access_token_here

# Repository Configuration
# Format: owner/repository-name (e.g., "dukeblue1994-glitch/cxi-project")
REPO=your-username/your-repo

# Git Branch for Feedback Storage
# The branch where feedback data will be committed
BRANCH=main

# Feedback Data File Path
# Path within the repository where feedback JSON will be stored
FEEDBACK_PATH=data/feedbacks.json

# Optional: Custom API Base URL (Advanced users only)
# Leave commented out to use default GitHub API
# GITHUB_API_BASE=https://api.github.com

# Development Settings (Local only - not needed in Netlify)
# PORT=8888
# NODE_ENV=development